.logo-image {
    width: 50px;
    height: 50px;
    object-fit: contain;
}

/* Gardez le fallback pour l'emoji */
.logo-icon {
    display: none; /* Cache l'emoji si image présente */
}

.logo-image:not([src]) + .logo-icon {
    display: flex; /* Affiche l'emoji si image manquante */
}
